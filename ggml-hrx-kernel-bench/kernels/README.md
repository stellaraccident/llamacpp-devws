# Kernels

Place Loom kernel sources owned by this workbench here.

The initial scaffold intentionally does not copy the HRX2 corpus. Importing
those files should be done as an explicit provenance-preserving step so each
kernel can record its original source repository, branch, commit, and any local
adaptations.
